<%= form_for(@issue) do |f| %>
  <% if @issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue.errors.count, "error") %> prohibited this issue from being saved:</h2>

      <ul>
      <% @issue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :problem %><br />
    <%= f.text_area :problem %>
  </div>
  <div class="field">
    <%= f.label :cause_id %><br />
    <%= f.number_field :cause_id %>
  </div>
  <div class="field">
    <%= f.label :effect_id %><br />
    <%= f.number_field :effect_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
