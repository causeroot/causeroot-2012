<%= form_for(@drilldown_rule) do |f| %>
  <% if @drilldown_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drilldown_rule.errors.count, "error") %> prohibited this drilldown_rule from being saved:</h2>

      <ul>
      <% @drilldown_rule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
